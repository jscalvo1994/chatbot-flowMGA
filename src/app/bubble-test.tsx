//import MessageBubble from "./components/messages/message-bubble";

async function BubbleTest() {
  await new Promise((resolve) => setTimeout(resolve, 5000));
  //return <MessageBubble text="Hello world!" />;
}

export default BubbleTest;
